<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#fcfcfc">

    <!-- 상단 플로팅 헤더 -->
    <TextView
        android:id="@+id/tvMainHeader"
        android:layout_width="match_parent"
        android:layout_height="75dp"
        android:text="오늘도 AI 프롬프트 실력 업! 🌱"
        android:textSize="22sp"
        android:textStyle="bold"
        android:textColor="#F4F1EA"
        android:background="@drawable/rounded_bottom_header"
        android:elevation="6dp"
        android:paddingStart="20dp"
        android:paddingTop="16dp"/>

    <!-- 전체 스크롤 영역 -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/tvMainHeader"
        android:fillViewport="true">

        <LinearLayout
            android:id="@+id/layoutContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="20dp">

            <!-- question_layout 5개 -->
            <include layout="@layout/question_layout" android:id="@+id/mainQuestion1"/>
            <include layout="@layout/question_layout" android:id="@+id/mainQuestion2"/>
            <include layout="@layout/question_layout" android:id="@+id/mainQuestion3"/>
            <include layout="@layout/question_layout" android:id="@+id/mainQuestion4"/>
            <include layout="@layout/question_layout" android:id="@+id/mainQuestion5"/>

        </LinearLayout>
    </ScrollView>

</RelativeLayout>
